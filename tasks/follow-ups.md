# Follow-ups

Backlog of out-of-scope items deferred from PRs. Pull from here when starting a fresh PR or planning Phase N+1 — don't slip them into unrelated work.

## From PR #3 (Phase 1 cleanup)

- **Stale "Última Convocatoria" copy on homepage** — `code/src/app/page.tsx:141-150` calls Julio 2024 the "latest call." It's May 2026; the badge implies recency it doesn't have. Fix: either update the date to the genuinely latest convocatoria, or rename the badge to something like `Convocatoria de prueba: Julio 2024` so the historical framing is explicit. Source: [CodeRabbit review on PR #3](https://github.com/craftwerk-studio/testnauti/pull/3#pullrequestreview-4216858399).
- **Tighten `NauticalSchool.status` to a string-literal union** — `code/src/types/directory.ts:22` is currently `status?: string`, so typos compile and slip through the `!== 'Inactive'` filter. CodeRabbit suggests `'Active' | 'Inactive' | 'Pending'`. Deferred because narrowing the type forces the Notion mapper (`code/src/lib/notion/fetchSchools.ts:165`) to validate/coerce, which is a wider change than the Phase 1 cleanup intended. The DirectoryKit plan already targets the narrowed shape (`tasks/directory-kit-extraction.md:101`), so do this when extracting the kit. Source: [CodeRabbit comment](https://github.com/craftwerk-studio/testnauti/pull/3#discussion_r3178792167) (note: actual line in `directory.ts`).
- **Decouple `ClaimUpdateForm` from TestNauti branding** — `code/src/app/escuelas/[schoolId]/actualizar/ClaimUpdateForm.tsx:25,103-105,376-383` hardcodes the Calendly URL fallback and the `escuelas@testnauti.co` support email. For Phase 2, lift these to props (or to `siteConfig` / page-level defaults) so the form ships into DirectoryKit without nautical baggage. Defaults live at the page boundary, not inside the reusable component. CodeRabbit flagged this against our own `.coderabbit.yaml` path-instruction. Land it during Phase 2 extraction (the inventory in `phase2-inventory.md` should pick this up). Source: [CodeRabbit comment](https://github.com/craftwerk-studio/testnauti/pull/3#pullrequestreview-4216866435).
